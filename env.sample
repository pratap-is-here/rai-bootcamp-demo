# ========================================
# RAI Bootcamp Demo - Environment Configuration
# ========================================

# === INFERENCE RESOURCE (Main Chat LLM) ===
# Azure OpenAI deployment for chat inference (East US)
AZURE_OPENAI_ENDPOINT=https://<your-resource-name>.openai.azure.com/
AZURE_OPENAI_CHAT_DEPLOYMENT_NAME=gpt-4o-mini

# Azure OpenAI API Version
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# === EVALUATION RESOURCE (Safety Evaluations) ===
# Azure AI project for Safety Evaluations (must be in supported region: East US 2, Sweden Central, etc.)
EVAL_OPENAI_ENDPOINT=https://<your-eval-resource-name>.openai.azure.com/
# Hub (resource) name in Azure AI Studio
EVAL_AZURE_AI_HUB_NAME=<your-hub-name>
# Project name under the hub (please set to your actual project resource name)
EVAL_AZURE_AI_PROJECT_NAME=<your-project-name>
# Resource group and subscription for the hub/project
EVAL_AZURE_RESOURCE_GROUP=<your-resource-group>
EVAL_AZURE_SUBSCRIPTION_ID=<your-subscription-id>

# === SHAREPOINT SOURCES (Configurable Web Sources) ===
# Override with comma-separated URLs if needed, otherwise uses config/sources.json
# CONFIG_SOURCES_URL=url1,url2,url3

# === OPTIONAL: Local Data Cache ===
# Path to cache fetched SharePoint pages
DATA_CACHE_PATH=./data_cache.json

# === OPTIONAL: Logging ===
LOG_LEVEL=INFO
